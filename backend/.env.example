# wx_note 环境变量
# 复制此文件为 .env 并填入实际值

# JWT 签名密钥（必填 — 设为 64+ 字符的随机字符串）
JWT_SECRET=change-me-to-a-random-64-char-string

# 服务监听端口
PORT=8100

# 数据存储目录（SQLite 数据库文件位置）
DATA_DIR=./data

# CORS 允许来源（生产环境设为前端 URL，如 https://wxnote.example.com）
# 留空或 * 表示允许所有来源（仅开发环境用）
CORS_ORIGIN=*
